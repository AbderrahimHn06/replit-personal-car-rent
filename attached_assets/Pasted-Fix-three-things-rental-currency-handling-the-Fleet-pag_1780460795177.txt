Fix three things: rental currency handling, the Fleet page local header toolbar, and make sure the rental flow actually respects the main currency.

IMPORTANT
- Frontend only
- Mock data only
- No backend
- No API calls
- Use local state only
- Keep the current design system
- Do not redesign the whole app, only fix behavior and layout quality

====================================================
1) RENTAL CURRENCY MUST WORK EVERYWHERE
====================================================

Current issue:
When creating a rent from any place in the app:
- Rentals page
- Availability page
- Client profile
- Any other rental modal

the agent must be able to choose the currency, and the app must respect the main currency from Settings.

Required behavior:
- The rental creation modal must include a currency selector
- Default selected currency = main currency from Settings
- If Settings main currency is DZD, the modal should default to DZD
- The agent can change currency inside the modal
- The selected currency must be used in the rental summary and total price display
- All rental creation entry points must use the same rental modal and the same currency logic

Currencies for now:
- DZD
- USD
- EUR

Car price model:
- Each car must store prices for all currencies
- Example:
  - DZD price
  - USD price
  - EUR price

Rental modal pricing:
- Show the price in the selected currency
- Recalculate estimated total using the selected currency price
- If the currency changes, all displayed amounts in the modal must update immediately

Important:
- Do not hardcode one currency
- Do not let the modal ignore the selected currency
- The rental modal must respect the main currency from Settings by default, but still let the user choose another one

UI in modal:
- Add a compact currency selector near the pricing section
- Show the currency code clearly
- Keep it visually consistent with the current modal style

====================================================
2) FIX THE FLEET PAGE TOP BAR / TOOLBAR
====================================================

Current issue:
The top bar area inside the Fleet page looks ugly and unbalanced.
The search bar and buttons are poorly placed.

I mean the Fleet page local header area, not the universal navbar.

Required layout:
- The page header should feel clean, aligned, and premium
- Title and subtitle on the left
- Search, filters, and Add Vehicle on the right
- Make the spacing balanced and consistent

Desktop layout:
- Left side:
  - Fleet title
  - short subtitle
- Right side:
  - local Fleet search bar
  - Filters button
  - Add Vehicle button

Search bar:
- should have a clean fixed height
- should not feel too wide or too narrow
- should align nicely with the buttons
- should have the same vertical height as the buttons

Filters button:
- same height as the search input
- aligned neatly beside it

Add Vehicle button:
- strong primary action
- same vertical alignment
- not floating awkwardly
- should sit at the far right cleanly

The whole toolbar should look like one grouped control row, not scattered elements.

Mobile behavior:
- Stack neatly
- Search first
- Then filters
- Then Add Vehicle
- Keep everything full width or close to full width
- No crowded layout
- No strange gaps

Visual rules:
- use consistent heights
- use consistent padding
- use flex and gap properly
- no awkward vertical offsets
- no misaligned controls

====================================================
3) FIX THE FLEET HEADER FEEL
====================================================

The Fleet page header should feel premium and readable.

Required structure:
- Page title
- Subtitle
- right-aligned local search and actions
- optional small filter chips below if needed

Do not make the header feel heavy.
Do not let the search bar dominate the layout.
Do not make the buttons feel detached from the search input.

====================================================
4) GENERAL UX REQUIREMENT
====================================================

Across the rental creation flow:
- Currency must be selectable and functional
- Main currency from Settings must be respected
- All create-rent entry points must behave the same way

Across the Fleet page:
- The top toolbar must look clean, aligned, and professional
- The search bar and buttons must sit properly together
- The page should feel polished and not awkward

====================================================
5) FINAL GOAL
====================================================

Make the rental currency workflow coherent across the whole app, and make the Fleet page top toolbar look properly designed, balanced, and premium.