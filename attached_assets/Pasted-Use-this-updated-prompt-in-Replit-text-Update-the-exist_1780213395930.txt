Use this updated prompt in Replit:

```text
Update the existing project with the following changes. Keep everything front-end only, simple, and clean.

1. Theme (IMPORTANT)
- Default theme must be LIGHT mode (not dark)
- Use a clean white/light gray background
- Use a premium accent color (gold or warm beige)
- Text should be dark (black/gray)
- Keep contrast high and readable
- Remove dark-mode-first styling

2. User Experience (flow)
Design the UX like this:

- User lands on Home page
- Immediately sees a horizontal booking form (main focus)
- User fills:
  - Pickup location
  - Return location
  - Pickup date + time
  - Return date + time
- When user clicks "Search" or "See vehicles":
  → Show available cars section BELOW the form (same page, no backend)
  → Filter cars based on selected dates (just simulate with mock logic)
- User can:
  - View cars
  - See price, name, image
  - Click “Book now” (no real booking, just UI)

3. Booking Form Layout (VERY IMPORTANT)
- Make the form HORIZONTAL like safarelamir.com
- Single row on desktop:
  [Pickup] [Return] [Pickup Date] [Pickup Time] [Return Date] [Return Time] [Button]

- On mobile:
  - Stack vertically
  - Keep spacing clean

- Style:
  - Rounded inputs
  - Soft borders
  - Light background (slightly gray)
  - Large CTA button on the right

4. Use this as reference (DO NOT COPY, just understand layout):
https://www.safarelamir.com/

Key idea:
- Wide horizontal search bar
- Compact inputs
- Strong CTA button

5. Cars Display After Search
- Initially show only hero + form
- After clicking search:
  - Show "Available Cars" section
  - Display 4–8 car cards (mock data)
  - Each card:
    - Image
    - Name
    - Price per day
    - Short specs
    - “Book now” button

6. Keep it minimal
- No backend
- No routing needed (single page is fine)
- No auth
- No API calls
- Use local state only

7. Code constraints
- Keep components small and reusable
- Avoid unnecessary complexity
- Do not over-engineer
- Do not add extra features

Goal:
A clean, light-themed car rental UI where the main focus is a horizontal booking form and showing available cars after search.
```
